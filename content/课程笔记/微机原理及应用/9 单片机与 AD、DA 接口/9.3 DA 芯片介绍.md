
事实上，有更加成熟的封装产品供我们使用。例如 **DAC0832** 。它的内部结构可以概括为：

![[Pasted image 20260601190728.png]]

我们注意到它有**两级锁存器**。这两级锁存器的锁存使能控制脚都放在左下角了，且都是高电平进行锁存。因此，这两级锁存器任你控制，你可以将它们全部接地，这样它就相当于一个裸的 D/A 片，你可以和我们之前说的那样直接接在 P1 上。

![[Pasted image 20260601191213.png]]



我们来看有哪些接法。首先最简单的一种：**直通式接法**。那么就是直接把 0832 当作裸 D/A 片来用，那么这时不能挂在外部总线上，因为没有锁存器了，外部总线数据是多方共用、不停变化的，只能直接接 `Pn` 使用。

![[Pasted image 20260601191346.png]]


**单缓冲式接法**。这种接法只启用其中一级缓冲，适合在外部总线上使用。

![[Pasted image 20260601191431.png]]



**双缓冲式接法**。双缓冲式接法主要解决的问题是，如果你需要在多片 D/A 上进行输出，那么由于数据线数量有限，你必须轮询，而轮询就会导致时间差异，如果只有一级缓冲，那么轮询时虽然保证了单片上的输出稳定，但是不同片之间存在先后差异。若你希望消除这种差异，让所有 D/A 片在一遍轮询完毕后同时输出，那么就需要使用双缓冲式接法——第一级缓冲用来锁住数据，第二级缓冲用来统一提交。

![[Pasted image 20260601191657.png]]

![[Pasted image 20260601191713.png]]

上图展现了一种双缓冲的应用场景：存在 $v_{x},v_{y}$ 两个输出需求（比如画图），这时片选通过译码器分出了三根线，两根线各自控制单片第一级锁存，一根线控制两片的共同第二级锁存。





### 12 位 D/A 接口

为了提高模拟信号的分辨率，我们会需要更多位数的 D/A 转换。但是，51 只能一次输出 8 位数据，因此多于 8 位的输出必然是需要轮询的。比如，看下面这种方案，它是否良好？

![[Pasted image 20260601192015.png]]

它的思路很直接，通过片选控制不同的部分，然后分时写入。它的问题和刚刚的多片 D/A 接法一样：存在时间差异。这种接法下先送入一部分，再送入另一部分，会导致最终输出信号有 3 个状态，即存在毛刺。

所以我们这样改装：

![[Pasted image 20260601192249.png]]

先写低 8 位，但是低 8 位先锁在第一级缓冲。后面写好高 4 位后，再同时把低 8 位的第二级锁存和高 4 位的锁存打开，统一提交。

下面看封装产品： **DAC1208** 。

![[Pasted image 20260601192525.png]]

它的锁存安排和我们前面说的不太一样，不过也很好理解。注意它的 12 位拆分是高 8 位和低 4 位，和上面说的不同。

我们观察到这个  $\text{BYTE1}/\overline{\text{BYTE2}}$  从标注上它的意思是高电平使第 1 字节打通/不锁存，低电平使第 2 字节打通/不锁存。

因为你可以直接控制第一级的两个寄存器是否打开，因此如果你不需要同时考虑多片之间的统一，那么 12 位寄存器和 4 位寄存器可以同时开闭，因为先写高 8 位，写好后只要低 4 位写好，就可以提交数据。如果要同步，那么把 12 位寄存器独立到第 3 次同步提交。

基于此，它的接法典型来看是：

![[Pasted image 20260601193228.png]]

